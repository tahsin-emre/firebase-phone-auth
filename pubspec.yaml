name: phone_login
description: "Firebase Phone Authentication and Login UI"
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: any
  firebase_auth: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any
  
flutter:
  uses-material-design: true